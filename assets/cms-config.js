/* Outliers at Play · CMS connection.
   Paste your Sanity Project ID below (from https://www.sanity.io/manage).
   Until this is set, the site falls back to the built-in static content. */
window.SANITY = {
  projectId: 'YOUR_PROJECT_ID',
  dataset: 'production',
};
